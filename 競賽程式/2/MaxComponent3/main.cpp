#include <iostream>
#define MAX_N 50000
#define MAX_M 1000000
using namespace std;

int main()
{
    int nodeWeight[MAX_N];
    int c, n, m, n1, n2;

    cin >> c;
    while(c-- > 0)
    {
        //initialize

        cin >> n >> m;
        for(int i = 0; i < n; i++)cin >> nodeWeight[i];
        for(int i = 0; i < m; i++)
        {
            cin >> n1 >> n2;
        }
    }


    return 0;
}
